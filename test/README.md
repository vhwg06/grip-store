# Test repository

This directory is the test-repository layout. Gherkin source is kept in
`specs/features`; Playwright/bddgen output is generated outside this source
tree and must not be committed here.
