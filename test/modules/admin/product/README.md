# Admin product UI

This vertical slice owns the operator browser journey for product creation,
media preview, and product-list actions. API product persistence is owned by
`catalog.product`; this module verifies the admin UI’s interaction contract
against that API-backed state.
