# Content Coverage

## Current Coverage

- `/admin/banners`
- `/admin/articles`
- `/admin/faqs`
- shared media uploader/library internals used from contextual editors
- public `/about` projection sourced from article ownership

## Discovery Notes

- these surfaces already behave like one coherent editorial/content domain
- previous docs split the behavior across historical modules, but the current inventory supports a dedicated content package
- `/admin/about`, `/admin/media`, and `/admin/product-content` are removed in the simplification pass and should not remain as active coverage targets
